<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movie Catalog</title>
  </head>
  <body>
    <h1>Actor List</h1>
    <a href="/movies">List of Movies</a>
    <table>
      <tr>
        <th>Actor Name</th>
      </tr>
      <% actors.each do |actor| %>
        <tr>
          <td><a href="/actors/<%= actor.id %>"><%= actor.name %>(<%= actor.num_movies %> movies)</a></td>
          <td><%= actor.character %></a></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
